import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true, // Ensures strict mode for better debugging
  //experimental: {
   // appDir: true, // Required if you're using the new `/app` directory structure
 // },
};

export default nextConfig;
